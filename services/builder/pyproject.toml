[project]
name = "snake-arena-builder"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "docker>=7.0",
    "snake-arena-common",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"


[tool.hatch.build.targets.wheel]
packages = ["builder"]