# Production Clerk instance — used by `vite build` automatically.
# Dashboard -> API keys -> Publishable key (pk_live_...).
VITE_CLERK_PUBLISHABLE_KEY=pk_live_Y2xlcmsuZ3JpZHNuYWtlLmNvbSQ

# Production API is served from the same origin (nginx proxy), so no base URL needed.
VITE_API_BASE_URL=/api
